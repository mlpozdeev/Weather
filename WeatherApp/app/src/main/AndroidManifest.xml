<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wheatherapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.weatherapp.WeatherApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WheatherApp"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.example.weatherapp.presentation.activities.weather.view.WeatherActivity"
            android:exported="true" />
        <activity
            android:name="com.example.weatherapp.presentation.activities.cities.view.CitiesActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>